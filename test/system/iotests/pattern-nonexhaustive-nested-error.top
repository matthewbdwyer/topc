// Expected: compile fails with "not exhaustive" (Empty arm missing).
type Inner = Lit(n) | Neg(x);
type Outer = Empty | Wrap(inner);
f(o) {
  var r;
  r = 0;
  case o of {
    Wrap(Lit(_)) -> r = 1;
  }
  return r;
}
main() { return f(Wrap(Lit(1))); }
