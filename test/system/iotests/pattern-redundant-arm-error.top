// Expected: compile fails with "unreachable case arm" (Some(v) already covers all Some).
type Opt = None | Some(val);
f(x) {
  var r;
  r = 0;
  case x of {
    None    -> r = 0;
    Some(v) -> r = v;
    Some(_) -> r = -1;
  }
  return r;
}
main() { return f(None); }
