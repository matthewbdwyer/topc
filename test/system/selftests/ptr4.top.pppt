increment(p) 
{
  *p = (*p + 1);
  return p;
}

main() 
{
  var x;
  x = alloc 42;
  x = increment(x);
  if ((*x != 43)) 
    error *x;
  return 0;
}

Functions : {
  increment : (⭡int) -> ⭡int,
  main : () -> int
}

Locals for function increment : {
  p : ⭡int
}

Locals for function main : {
  x : ⭡int
}
