main() 
{
  var x, y;
  x = alloc 42;
  y = alloc 17;
  if ((*x != 42)) 
    error *x;
  if ((*y != 17)) 
    error *y;
  return 0;
}

Functions : {
  main : () -> int
}

Locals for function main : {
  x : ⭡int,
  y : ⭡int
}
