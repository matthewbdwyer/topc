// soundness grid case own.seq.read.ident
// kind=own context=seq op1=read op2=ident
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
ident(z) { return z; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  r = r + *x;
  y = ident(x);
  return 0;
}
