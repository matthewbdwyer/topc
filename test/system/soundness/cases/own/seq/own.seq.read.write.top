// soundness grid case own.seq.read.write
// kind=own context=seq op1=read op2=write
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  r = r + *x;
  *x = 3;
  return 0;
}
