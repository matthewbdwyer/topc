// soundness grid case own.seq.assign.temp
// kind=own context=seq op1=assign op2=temp
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
make() { var p; p = alloc 7; return p; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  y = x;
  r = r + *make();
  return 0;
}
