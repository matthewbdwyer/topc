// soundness grid case own.seq.ident.none
// kind=own context=seq op1=ident op2=none
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
ident(z) { return z; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  y = ident(x);
  
  return 0;
}
