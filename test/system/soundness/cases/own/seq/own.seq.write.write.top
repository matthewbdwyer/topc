// soundness grid case own.seq.write.write
// kind=own context=seq op1=write op2=write
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  *x = 3;
  *x = 3;
  return 0;
}
