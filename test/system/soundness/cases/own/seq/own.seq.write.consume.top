// soundness grid case own.seq.write.consume
// kind=own context=seq op1=write op2=consume
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
consume(p) { return *p; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  *x = 3;
  r = r + consume(x);
  return 0;
}
