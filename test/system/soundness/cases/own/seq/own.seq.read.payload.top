// soundness grid case own.seq.read.payload
// kind=own context=seq op1=read op2=payload
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  r = r + *x;
  w = Wrap(x);
  return 0;
}
