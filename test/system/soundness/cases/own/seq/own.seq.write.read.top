// soundness grid case own.seq.write.read
// kind=own context=seq op1=write op2=read
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  *x = 3;
  r = r + *x;
  return 0;
}
