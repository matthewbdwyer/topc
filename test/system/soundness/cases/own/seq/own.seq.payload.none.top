// soundness grid case own.seq.payload.none
// kind=own context=seq op1=payload op2=none
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  w = Wrap(x);
  
  return 0;
}
