// soundness grid case own.if2.assign.none
// kind=own context=if2 op1=assign op2=none
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  if (c > 0) { y = x; } else { y = x; }
  
  return 0;
}
