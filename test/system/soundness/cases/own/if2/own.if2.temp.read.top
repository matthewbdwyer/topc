// soundness grid case own.if2.temp.read
// kind=own context=if2 op1=temp op2=read
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
make() { var p; p = alloc 7; return p; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  if (c > 0) { r = r + *make(); } else { r = r + *make(); }
  r = r + *x;
  return 0;
}
