// soundness grid case own.if1.payload.write
// kind=own context=if1 op1=payload op2=write
// expect: reject (ownership state disagreement at control-flow join)
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  if (c > 0) { w = Wrap(x); }
  *x = 3;
  return 0;
}
