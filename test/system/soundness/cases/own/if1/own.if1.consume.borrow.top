// soundness grid case own.if1.consume.borrow
// kind=own context=if1 op1=consume op2=borrow
// expect: reject (ownership state disagreement at control-flow join)
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
consume(p) { return *p; }
look(q) { return **q; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  if (c > 0) { r = r + consume(x); }
  r = r + look(&x);
  return 0;
}
