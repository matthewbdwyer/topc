// soundness grid case own.if1.write.payload
// kind=own context=if1 op1=write op2=payload
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  if (c > 0) { *x = 3; }
  w = Wrap(x);
  return 0;
}
