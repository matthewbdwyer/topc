// soundness grid case own.expr.temp.temp
// kind=own context=expr op1=temp op2=temp
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
make() { var p; p = alloc 7; return p; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  r = r + *make() + *make();
  return 0;
}
