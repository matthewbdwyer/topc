// soundness grid case own.expr.temp.borrow
// kind=own context=expr op1=temp op2=borrow
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
look(q) { return **q; }
make() { var p; p = alloc 7; return p; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  r = r + *make() + look(&x);
  return 0;
}
