// soundness grid case own.cond.read.payload
// kind=own context=cond op1=read op2=payload
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  i = 0;
  while (*x > i) { i = i + 1000; }
  w = Wrap(x);
  return 0;
}
