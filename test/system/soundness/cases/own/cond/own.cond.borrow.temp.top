// soundness grid case own.cond.borrow.temp
// kind=own context=cond op1=borrow op2=temp
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
look(q) { return **q; }
make() { var p; p = alloc 7; return p; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  i = 0;
  while (look(&x) > i) { i = i + 1000; }
  r = r + *make();
  return 0;
}
