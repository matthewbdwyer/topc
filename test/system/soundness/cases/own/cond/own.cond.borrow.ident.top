// soundness grid case own.cond.borrow.ident
// kind=own context=cond op1=borrow op2=ident
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
ident(z) { return z; }
look(q) { return **q; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  i = 0;
  while (look(&x) > i) { i = i + 1000; }
  y = ident(x);
  return 0;
}
