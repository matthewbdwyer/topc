// soundness grid case own.cond.borrow.write
// kind=own context=cond op1=borrow op2=write
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
look(q) { return **q; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  i = 0;
  while (look(&x) > i) { i = i + 1000; }
  *x = 3;
  return 0;
}
