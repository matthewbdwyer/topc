// soundness grid case own.arm1.assign.borrow
// kind=own context=arm1 op1=assign op2=borrow
// expect: reject (ownership state disagreement at control-flow join)
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
look(q) { return **q; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = alloc 5;
  sel = On(1);
  case sel of { On(k) -> { y = x; } Off -> r = r + 0; }
  r = r + look(&x);
  return 0;
}
