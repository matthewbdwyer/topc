// soundness grid case int.if2.assign.payload
// kind=int context=if2 op1=assign op2=payload
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  if (c > 0) { y = x; } else { y = x; }
  w = Wrap(x);
  return 0;
}
