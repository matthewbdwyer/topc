// soundness grid case int.if2.ident.consume
// kind=int context=if2 op1=ident op2=consume
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
ident(z) { return z; }
consume(p) { return p + 1; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  if (c > 0) { y = ident(x); } else { y = ident(x); }
  r = r + consume(x);
  return 0;
}
