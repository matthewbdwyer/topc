// soundness grid case int.if2.consume.ident
// kind=int context=if2 op1=consume op2=ident
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
ident(z) { return z; }
consume(p) { return p + 1; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  if (c > 0) { r = r + consume(x); } else { r = r + consume(x); }
  y = ident(x);
  return 0;
}
