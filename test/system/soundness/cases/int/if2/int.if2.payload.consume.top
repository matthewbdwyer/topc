// soundness grid case int.if2.payload.consume
// kind=int context=if2 op1=payload op2=consume
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
consume(p) { return p + 1; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  if (c > 0) { w = Wrap(x); } else { w = Wrap(x); }
  r = r + consume(x);
  return 0;
}
