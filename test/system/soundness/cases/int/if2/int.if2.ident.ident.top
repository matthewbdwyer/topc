// soundness grid case int.if2.ident.ident
// kind=int context=if2 op1=ident op2=ident
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
ident(z) { return z; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  if (c > 0) { y = ident(x); } else { y = ident(x); }
  y = ident(x);
  return 0;
}
