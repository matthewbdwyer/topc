// soundness grid case int.if2.assign.borrow
// kind=int context=if2 op1=assign op2=borrow
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
look(q) { return *q; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  if (c > 0) { y = x; } else { y = x; }
  r = r + look(&x);
  return 0;
}
