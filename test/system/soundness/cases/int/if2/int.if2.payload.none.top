// soundness grid case int.if2.payload.none
// kind=int context=if2 op1=payload op2=none
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  if (c > 0) { w = Wrap(x); } else { w = Wrap(x); }
  
  return 0;
}
