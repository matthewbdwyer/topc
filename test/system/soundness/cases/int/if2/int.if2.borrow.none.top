// soundness grid case int.if2.borrow.none
// kind=int context=if2 op1=borrow op2=none
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
look(q) { return *q; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  if (c > 0) { r = r + look(&x); } else { r = r + look(&x); }
  
  return 0;
}
