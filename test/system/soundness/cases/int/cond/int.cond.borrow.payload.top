// soundness grid case int.cond.borrow.payload
// kind=int context=cond op1=borrow op2=payload
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
look(q) { return *q; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  i = 0;
  while (look(&x) > i) { i = i + 1000; }
  w = Wrap(x);
  return 0;
}
