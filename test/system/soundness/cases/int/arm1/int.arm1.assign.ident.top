// soundness grid case int.arm1.assign.ident
// kind=int context=arm1 op1=assign op2=ident
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
ident(z) { return z; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  sel = On(1);
  case sel of { On(k) -> { y = x; } Off -> r = r + 0; }
  y = ident(x);
  return 0;
}
