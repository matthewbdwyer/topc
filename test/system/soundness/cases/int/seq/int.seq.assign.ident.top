// soundness grid case int.seq.assign.ident
// kind=int context=seq op1=assign op2=ident
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
ident(z) { return z; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  y = x;
  y = ident(x);
  return 0;
}
