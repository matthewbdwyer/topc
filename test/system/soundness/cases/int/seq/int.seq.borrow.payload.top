// soundness grid case int.seq.borrow.payload
// kind=int context=seq op1=borrow op2=payload
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
look(q) { return *q; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  r = r + look(&x);
  w = Wrap(x);
  return 0;
}
