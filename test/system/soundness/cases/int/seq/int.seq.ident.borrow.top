// soundness grid case int.seq.ident.borrow
// kind=int context=seq op1=ident op2=borrow
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
ident(z) { return z; }
look(q) { return *q; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  y = ident(x);
  r = r + look(&x);
  return 0;
}
