// soundness grid case int.seq.assign.none
// kind=int context=seq op1=assign op2=none
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  y = x;
  
  return 0;
}
