// soundness grid case int.seq.payload.payload
// kind=int context=seq op1=payload op2=payload
// expect: reject (assigned while still owned)
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  w = Wrap(x);
  w = Wrap(x);
  return 0;
}
