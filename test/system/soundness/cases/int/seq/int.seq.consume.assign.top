// soundness grid case int.seq.consume.assign
// kind=int context=seq op1=consume op2=assign
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
consume(p) { return p + 1; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  r = r + consume(x);
  y = x;
  return 0;
}
