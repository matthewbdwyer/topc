// soundness grid case int.seq.payload.borrow
// kind=int context=seq op1=payload op2=borrow
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
look(q) { return *q; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  w = Wrap(x);
  r = r + look(&x);
  return 0;
}
