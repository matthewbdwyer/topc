// soundness grid case int.seq.payload.assign
// kind=int context=seq op1=payload op2=assign
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  w = Wrap(x);
  y = x;
  return 0;
}
