// soundness grid case int.seq.assign.consume
// kind=int context=seq op1=assign op2=consume
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
consume(p) { return p + 1; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  y = x;
  r = r + consume(x);
  return 0;
}
