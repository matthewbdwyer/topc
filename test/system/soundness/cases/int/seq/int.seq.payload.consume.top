// soundness grid case int.seq.payload.consume
// kind=int context=seq op1=payload op2=consume
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
consume(p) { return p + 1; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  w = Wrap(x);
  r = r + consume(x);
  return 0;
}
