// soundness grid case int.if1.ident.payload
// kind=int context=if1 op1=ident op2=payload
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
ident(z) { return z; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  if (c > 0) { y = ident(x); }
  w = Wrap(x);
  return 0;
}
