// soundness grid case int.if1.consume.payload
// kind=int context=if1 op1=consume op2=payload
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
consume(p) { return p + 1; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  if (c > 0) { r = r + consume(x); }
  w = Wrap(x);
  return 0;
}
