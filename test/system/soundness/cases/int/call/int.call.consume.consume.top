// soundness grid case int.call.consume.consume
// kind=int context=call op1=consume op2=consume
// expect: accept
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
consume(p) { return p + 1; }
pair(a1, a2) { return consume(a1) + consume(a2); }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = 5;
  r = r + pair(x, x);
  return 0;
}
