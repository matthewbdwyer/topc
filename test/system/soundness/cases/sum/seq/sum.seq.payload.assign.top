// soundness grid case sum.seq.payload.assign
// kind=sum context=seq op1=payload op2=assign
// expect: reject (used after move | moved more than once | assigned while still owned)
type Cell = Val(n) | Nope;
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = Val(5);
  w = Wrap(x);
  y = x;
  return 0;
}
