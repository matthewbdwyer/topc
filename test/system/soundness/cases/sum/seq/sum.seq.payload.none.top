// soundness grid case sum.seq.payload.none
// kind=sum context=seq op1=payload op2=none
// expect: accept
type Cell = Val(n) | Nope;
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = Val(5);
  w = Wrap(x);
  
  return 0;
}
