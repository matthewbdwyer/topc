// soundness grid case sum.seq.ident.none
// kind=sum context=seq op1=ident op2=none
// expect: accept
type Cell = Val(n) | Nope;
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
ident(z) { return z; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = Val(5);
  y = ident(x);
  
  return 0;
}
