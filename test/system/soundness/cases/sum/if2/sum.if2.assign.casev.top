// soundness grid case sum.if2.assign.casev
// kind=sum context=if2 op1=assign op2=casev
// expect: reject (used after move | moved more than once | assigned while still owned)
type Cell = Val(n) | Nope;
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = Val(5);
  if (c > 0) { y = x; } else { y = x; }
  case x of { Val(n) -> r = r + n; Nope -> r = r + 0; }
  return 0;
}
