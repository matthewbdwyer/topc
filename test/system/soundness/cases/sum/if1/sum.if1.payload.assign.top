// soundness grid case sum.if1.payload.assign
// kind=sum context=if1 op1=payload op2=assign
// expect: reject (ownership state disagreement at control-flow join)
type Cell = Val(n) | Nope;
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = Val(5);
  if (c > 0) { w = Wrap(x); }
  y = x;
  return 0;
}
