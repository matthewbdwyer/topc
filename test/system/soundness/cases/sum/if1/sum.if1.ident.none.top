// soundness grid case sum.if1.ident.none
// kind=sum context=if1 op1=ident op2=none
// expect: reject (ownership state disagreement at control-flow join)
type Cell = Val(n) | Nope;
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
ident(z) { return z; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = Val(5);
  if (c > 0) { y = ident(x); }
  
  return 0;
}
