// soundness grid case sum.cond.consume.ident
// kind=sum context=cond op1=consume op2=ident
// expect: reject (move in while-loop condition)
type Cell = Val(n) | Nope;
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
ident(z) { return z; }
consume(p) { var t; t = 0; case p of { Val(n) -> t = n; Nope -> t = 0; } return t; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = Val(5);
  i = 0;
  while (consume(x) > i) { i = i + 1000; }
  y = ident(x);
  return 0;
}
