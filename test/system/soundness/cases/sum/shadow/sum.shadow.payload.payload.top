// soundness grid case sum.shadow.payload.payload
// kind=sum context=shadow op1=payload op2=payload
// expect: reject (used after move | moved more than once | assigned while still owned)
type Cell = Val(n) | Nope;
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = Val(5);
  y = Val(9);
  case y of { Val(n) -> { w = Wrap(x); w = Wrap(x); r = r + n; } Nope -> { w = Wrap(x); w = Wrap(x); } }
  return 0;
}
