// soundness grid case sum.shadow.casev.none
// kind=sum context=shadow op1=casev op2=none
// expect: accept
type Cell = Val(n) | Nope;
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = Val(5);
  y = Val(9);
  case y of { Val(n) -> { case x of { Val(n) -> r = r + n; Nope -> r = r + 0; }  r = r + n; } Nope -> { case x of { Val(n) -> r = r + n; Nope -> r = r + 0; }  } }
  return 0;
}
