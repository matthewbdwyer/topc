// soundness grid case sumown.cond.borroww.casev
// kind=sumown context=cond op1=borroww op2=casev
// expect: accept
type Box = Full(v) | Empty;
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
lookw(q) { var t; t = 0; case *q of { Full(_) -> t = 1; Empty -> t = 0; } return t; }
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = Full(alloc 5);
  i = 0;
  while (lookw(&x) > i) { i = i + 1000; }
  case x of { Full(v) -> r = r + *v; Empty -> r = r + 0; }
  return 0;
}
