// soundness grid case sumown.if1.assign.none
// kind=sumown context=if1 op1=assign op2=none
// expect: reject (ownership state disagreement at control-flow join)
type Box = Full(v) | Empty;
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = Full(alloc 5);
  if (c > 0) { y = x; }
  
  return 0;
}
