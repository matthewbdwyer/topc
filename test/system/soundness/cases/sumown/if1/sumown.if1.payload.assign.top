// soundness grid case sumown.if1.payload.assign
// kind=sumown context=if1 op1=payload op2=assign
// expect: reject (ownership state disagreement at control-flow join)
type Box = Full(v) | Empty;
type Wrapper = Wrap(inner);
type Sel = On(k) | Off;
main(c) {
  var x, y, w, r, i, sel;
  r = 0;
  x = Full(alloc 5);
  if (c > 0) { w = Wrap(x); }
  y = x;
  return 0;
}
