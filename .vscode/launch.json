{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "topc (debug)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/src/topc",
            "args": [
                "--help"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}